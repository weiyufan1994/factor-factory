# 公开源码验证范围

此公开分支来自已验收的 2026-09-22 源码包，并选择合入本地 IT 版本的可移植代码。个人机器路径、云存储与实例标识已改成示例值；私有研究和运维记录不交付。

本分支不包含独立 Data API SDK。SDK 专用准备模块、测试和远端 worker 脚本未纳入；原有 SDK 独立分钟生产示例保留。其他实际取数功能仍需要用户安装 SDK、提供目录和数据，不以模拟客户端替代实际部署。

## 本次验收

2026-09-22，在整理后的独立工作区实际执行完整公开版离线清单：**820 passed，2 deselected，0 skipped，38.16 秒**。其中新增纳入默认清单的实现运行、三种实现路径、PF/VV 冻结源码数值对照、Step5 声明结果消费与 Step3B 模板边界共 82 项。各项计数不与先前批次相加。

使用 macOS sandbox 禁止网络与源工程、Mac runtime、独立 SDK、已安装 Skill 的读取；仅允许临时测试目录写入。只运行合成测试，没有访问真实行情、OOS 或个人研究产物。环境为 macOS arm64、Python 3.14.3、pandas 2.3.3。已有依赖直接使用，未将此描述为全新在线安装验收。

有 6 条同一类 pandas GroupBy.apply 未来行为警告，来自三种实现路径的指标一致性测试；当前结果通过，未来 pandas 升级仍需复验。Ultimate 与 research-brain 的 Skill 结构检查通过，公开入口不包含本机 selector。

同一隔离环境从仓库目录外实际调用检索 CLI：数学方法查询命中 1 条，PF/VV 机制描述命中 5 条，未知查询返回 0 条且标为 cold-start。Ultimate 帮助入口正常，含 local IS 参数；这些是关键词/图谱消费者检查，不是 embedding 召回验收。经验自由文本中残留的 6 处具体研究数值已经移除，节点与边表同步，原私有文件哈希保持不变。

提交后另从 Git 跟踪内容导出到全新临时目录，重建包内索引，并额外禁止读取原公开工作区；再次完整运行 **820 passed，2 deselected，0 skipped，37.08 秒**，警告同上。这确认通过不依赖未跟踪的知识文件、原检索缓存或原 checkout。随后仅补写本段验证记录。

`pytest-offline.ini` 保留两项既有历史 Git 依赖的排除；除此之外不因公开版失败新增取消选择。Data API 专项需独立 SDK，属于额外集成验收，不冒充框架自身无依赖通过。

## 使用与限制

- 普通本地研究从 Ultimate 开始，分段停在代码评审节点再继续。
- 原文算法、重建假设和自主扩展分别记录。
- 真实研究经验的正确写回和跨案例效用，不能仅由合成测试证明。
- 公开检索不附带 embedding 模型或密钥。
- 本次未发布行情、原报告或真实账户/回测记录，未运行 OOS，未部署云端。
- 已有公开 Git 历史不在本次重写范围。
